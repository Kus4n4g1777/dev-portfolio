# This is a temporary placeholder Dockerfile for the Angular frontend.
# It will be replaced with a proper multi-stage build for Angular later.
FROM alpine:latest
# Just keep the container running
CMD ["tail", "-f", "/dev/null"]
