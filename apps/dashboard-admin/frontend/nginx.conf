server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Angular SPA — all routes serve index.html
    # and Angular's router takes it from there
    location / {
        try_files $uri $uri/ /index.html;
    }
}
